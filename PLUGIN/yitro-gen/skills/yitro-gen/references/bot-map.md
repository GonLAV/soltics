# מפת שני היישומים של תום

הכל בריפו `quadclinic`, תחת `.claude/skills/quad-bot/`. הנתיב לריפו עצמו משתנה בין מחשבים (`cd ~/quad-code` או חיפוש `quadclinic`).

## 1. הבוט ב-Make (הראשי, חי היום) — תרחיש 6152004

- **פרומפט:** `quad-os/ref/bot-prompt.md` (המקור), מוטמע בתרחיש עצמו ב-Make.
- **ערוץ:** GreenAPI, מספר 0525464644 (לידים).
- **מנוע פולואפ נפרד:** תרחיש 6157687 (FU1/FU2/FU3) — לא חלק מהצ'אט הנכנס.
- **בדיקת רגרסיה:** אין כלי אוטומטי. אחרי כל שינוי פרומפט — לקרוא מחדש את "תובנות 150 השיחות" ב-`05-בוט-וואטסאפ-והמשפך.md` ולבדוק ידנית מול כמה שיחות מהארכיון.
- **חוקי ברזל רלוונטיים:** מחירים (450 אבחון · 2,000-9,000 תוכנית, אסור 12,000/14,000), שעות (רק schedule.easybizy.net), 5 סטטוסים מוגנים, "אל תתנצל, תמשיך לשלוח לינק/מחיר/ביטוח".

## 2. הבוט העצמאי — `bot/` (נבנה 29/08/2026, על WHAPI)

- **פרסונה:** `bot/src/persona.js` — נשען על אותו `bot-prompt.md`, עם ה-patch של 22/08/2026 (2,000–9,000, לא 14,000).
- **בסיס ידע:** `bot/knowledge/*.md` — 7 קבצים, גרסה לקוח-פונה של `quad-os/`. **`quad-os` נשאר מקור האמת** — שינוי שם עדכנו שם, מעדכנים גם כאן.
- **ערוץ:** WHAPI, מספר 0559562302 (**לקוחות קיימים**, לא לידים).
- **בדיקת רגרסיה (חובה אחרי כל שינוי):** `npm run chat -- --suite` בתוך `.claude/skills/quad-bot/bot/` — 7 מלכודות בטיחות (מחיר, ביטוח, רפואי, הזרקה). חייב 7/7 לפני שמשחררים.
- **מה שם ואין ב-Make:** guardrails מפורשים על הפלט (בדיקה שאין מחיר מומצא לפני שליחה), race-gate להשתלטות ידנית, escalation מפורש.
- **מה אין שם בכוונה:** מנוע פולואפ — FU1/2/3 נשארים רק ב-Make.

## מתי תיקון נוגע בשניהם

מחיר, שעה, מדיניות עסקית, או כלל-ברזל חדש — צריך לעדכן **גם** `bot-prompt.md` **וגם** `bot/knowledge/*.md`/`persona.js`, אחרת שני היישומים יתפצלו בשקט ויתנו תשובות סותרות ללקוחות בשני המספרים. תיקון של ניסוח/טון בלבד יכול להישאר ביישום אחד אם הבעיה נצפתה רק שם.

## היכן חי הארכיון לדגימה

`.claude/skills/quad-bot/ALL_CHATS.txt` (מאוחד) · `ALL_CHATS_2025.txt` · `ALL_CHATS_2026.txt` — גיבוי iMazing מלא, לפי שיחה, מהחדש לישן. שימוש בחיפוש טקסט (מילת מפתח, מספר טלפון, תאריך) כדי לאתר מקרים — הקבצים גדולים מדי לקריאה מלאה בבת אחת.
